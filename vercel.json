{
    "version": 2,
    "builds": [
      { "src": "api/server.js", "use": "@now/node-server" }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "api/server.js" },
      { "src": "/(.*)", "dest": "/" }
    ]
  }
  