{
  "version": 2,
  "builds": [
    { "src": ".", "use": "@vercel/static-build" },
    { "src": "server/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server/api/$1" },
    { "src": "/(.*)", "dest": "$1" }
  ]
}
